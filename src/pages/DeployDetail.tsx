import React, { FC } from 'react';
import { RouteComponentProps } from '@reach/router';

export interface DeployDetailPageProps extends RouteComponentProps {

}

export const DeployDetailPage: FC<DeployDetailPageProps> = (props) => {
  // TODO: Bring in the Console, a heading, & a basic display of fixed details
  return <></>
}

export default DeployDetailPage;